# The ANSIBLE Server code

` This Directory contains the Dockerfile for ansible server `
 
Use the below command to build an image of name ansible-server:v1

```
docker build -t ansible-server:v1 .
```
